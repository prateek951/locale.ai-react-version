import React from "react";
import NavStyles from "./styles/NavStyles";
const routes = () => {
  return (
    <React.Fragment>
        {/* The links will go here */}
    </React.Fragment>
  );
};

const Nav = () => <NavStyles>{routes()}</NavStyles>;
export default Nav;
